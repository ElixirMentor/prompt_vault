defmodule PromptVault.MessageTest do
  use ExUnit.Case, async: true

  doctest PromptVault.Message

  # This file tests the Message behaviour itself
  # Individual message type tests are in their own files
end
