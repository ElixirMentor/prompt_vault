defmodule PromptVault.CompactionTest do
  use ExUnit.Case, async: true

  doctest PromptVault.Compaction

  # This file tests the Compaction behaviour itself
  # Individual compaction strategy implementation tests are in their own files
end
