defmodule PromptVault.TokenCounterTest do
  use ExUnit.Case, async: true

  doctest PromptVault.TokenCounter

  # This file tests the TokenCounter behaviour itself
  # Individual token counter implementation tests are in their own files
end
